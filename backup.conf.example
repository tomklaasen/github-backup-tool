BACKUP_DIR="/path/to/backup"
CLONE_PROTOCOL="https"  # "https" or "ssh"

# Logging (optional – leave LOG_FILE empty to print to stdout)
LOG_FILE=""              # e.g. "/var/log/github-backup.log"
LOG_MAX_SIZE_KB="10240"  # rotate when log exceeds this size (default 10 MB)
LOG_KEEP="5"             # number of rotated files to keep
