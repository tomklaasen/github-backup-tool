BACKUP_DIR="/path/to/backup"
CLONE_PROTOCOL="https"  # "https" or "ssh"

# Logging (optional – leave LOG_FILE empty to print to stdout)
LOG_FILE=""              # e.g. "/var/log/github-backup.log"
LOG_MAX_SIZE_KB="10240"  # rotate when log exceeds this size (default 10 MB)
LOG_KEEP="5"             # number of rotated files to keep

# Healthchecks.io (optional – leave empty to disable)
HC_PING_URL=""           # e.g. "https://hc-ping.com/your-uuid-here"

# Extra GitHub accounts (optional)
# Each entry is a Personal Access Token (PAT) with "repo" scope.
# Repos from these accounts are backed up alongside the main gh-authenticated account.
EXTRA_TOKENS=()
